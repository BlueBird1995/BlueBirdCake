<!-- ジャンル検索部分テンプレート -->

<h2>商品一覧（全'<%= @content %>'件）</h2>
<% @products.each do |product| %>
  <%= attachment_image_tag product_image, :image %>
  <%= product.name %>
  <%= product.price %>
<% end %>